# Mapa de dependencias externas — Fiabilidad de la sync

## Sources

- `bolt-plan.md`, `dependencies.md` (codekb).

## Dependencias externas

Ninguna dependencia externa nueva. El intent es aditivo y a coste 0 €:

- No añade paquetes (usa `pytest`/`pytest-cov` ya presentes).
- No toca integraciones externas (Futmondo/Sofascore) ni su contrato.
- No cambia infraestructura (Fly.io/Neon) ni el pipeline de despliegue.

## Assumptions & Open Questions

None.
