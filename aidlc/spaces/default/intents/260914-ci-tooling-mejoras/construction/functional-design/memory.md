<!-- INVARIANT: examples are single-line HTML comments so a fresh template parses to total=0 (MEMORY_EMPTY). Do NOT un-comment or split across lines. t100 guards this. -->
> This file is kept up to date automatically while the stage runs. Add observations at the review step, not by editing here directly.

## Interpretations
- 2026-09-14T11:32:00Z — Scope refactor: units-generation y domain-design saltados por diseño (consumes_absent expected:true). No hay DAG de Unidades, así que es una iteración de etapa ordinaria sin ceremonia de Bolt/per-Unit. Se trata la estructura de código existente (codekb) como diseño de dominio de facto.
- 2026-09-14T11:32:00Z — Las 5 mejoras son de CI/tooling/dependencias/config, sin lógica de negocio ni entidades de dominio nuevas. entities.md documentará explícitamente que no hay entidades de dominio (el "modelo" son artefactos de configuración: workflows, package.json, .nvmrc). rules.md capturará las reglas técnicas verificables de cada mejora como BRx.y. functional-spec.md será la fuente de verdad de los flujos/secuencia de aplicación de cada mejora. frontend-components.md (UI) no aplica: ningún cambio de componente de UI (la migración de animaciones toca API, no jerarquía de componentes).
- 2026-09-14T11:32:00Z — Profundidad Minimal + requisitos ya muy detallados (FR1-FR6 con criterios) => las decisiones están tomadas en Requirements; preguntas de esta etapa mínimas/excepcionales (Construction).
<!-- example: 2026-05-29T10:14:32Z — chose REST over GraphQL; the consuming team only needs CRUD, revisit if subscriptions land -->

## Deviations
<!-- example: 2026-05-29T10:14:32Z — skipped the optional caching layer the stage prose suggested; the dataset is small enough that it adds risk -->

## Tradeoffs
- 2026-09-14T11:33:00Z — FR3 (animaciones): inspección del código = 0 imports/triggers de la API antigua de @angular/animations; el único uso es provideAnimationsAsync() en app.config.ts. PERO Angular Material se usa en 37 ficheros y su UX (ripples, menús, tooltips, overlays) depende del sistema de animaciones habilitado por ese provider. Retirar provideAnimationsAsync()/@angular/animations degradaría las animaciones de Material. => El diseño se inclina por "sin acción de migración; conservar el provider y la dependencia porque los necesita Material", en lugar de retirarlos. Se confirma con el humano por ser una decisión de alcance de FR3.
- 2026-09-14T11:33:00Z — FR1 (actions): checkout@v5, setup-python@v5, gitleaks-action@v2 ya usan Node 24; el objetivo real es actions/setup-node@v4 -> última major (v5) en ci.yml y fly-deploy.yml. No existe ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION (FR1.2 se cumple trivialmente). superfly/flyctl-actions/setup-flyctl@master y browser-actions/setup-chrome@v1 a revisar.
- 2026-09-14T11:33:00Z — FR4 (Karma->Vitest): angular.json test builder = @angular/build:unit-test runner:karma browsers:[ChromeHeadless]; devDeps Karma = karma, karma-chrome-launcher, karma-coverage, karma-jasmine, karma-jasmine-html-reporter (+ jasmine-core, @types/jasmine a evaluar). Nota: los workflows de CI ejecutan `ng test --browsers=ChromeHeadless` e instalan Chrome headless; migrar a Vitest puede permitir simplificar esos pasos (Vitest usa jsdom/happy-dom por defecto) — a considerar pero fuera del criterio mínimo de FR4.
- 2026-09-14T11:33:00Z — FR5 (Node local): no existe .nvmrc; package.json sin campo engines; packageManager npm@11.12.1.
<!-- example: 2026-05-29T10:14:32Z — picked TDD over BDD this run; the team is unit-first and the domain is well-understood -->

## Open questions
- 2026-09-14T11:58:00Z — Bug del motor (v2.8.2) en summaryQuestionFiles: una etapa for_each: unit-of-work sin Unit DAG (scope refactor) bloqueaba el arranque del revisor con SUMMARY_QUESTIONS_MISSING porque el guard solo miraba construction/<unit>/<slug>/ y no el fallback stage-level construction/<slug>/. Se aplicó el fix en el fuente (/home/javi/aidlc-workflows) y se recompiló/instaló el binario parcheado (backup en ~/.local/share/aidlc/versions/2.8.2/aidlc.bak). Pendiente: subir el fix como PR al proyecto oficial. Los tests t328/t320 pasan; fallos de t186/t331/gates bolt-swarm son preexistentes del entorno (Bun 1.4.2 vs 1.3.14).
- 2026-09-14T11:58:00Z — Hallazgos advisory del revisor de arquitectura (no bloquean, READY): R-01 (Major) NFR3->BR5.1 y NFR5->BR4.1/BR4.2 ausentes de traceability.json (hueco de trazado); R-02 trazado a FR/NFR y no a IDs AC (requirements no tiene IDs AC estables); R-03 BR1.1 vs BR1.3 (setup-flyctl/setup-chrome discrecionales); R-04 config muerta de Chrome headless en CI tras Vitest; R-05 destino de la nota de punycode sin anclar.
<!-- example: 2026-05-29T10:14:32Z — confirm the retention window with compliance before the next stage hardens the schema -->
